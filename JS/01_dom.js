// select parent element and find out values in it
let parentElement;

parentElement = document.querySelector('.parent')
console.log(parentElement);  //gives the element

console.log(parentElement.children);

